# Local development:
# Leave VITE_API_URL UNSET. The Vite dev server proxies /api -> 127.0.0.1:4000
# (see vite.config.js).

# Production (Vercel):
# Set this to the public origin of the Railway API service, no trailing slash.
# VITE_API_URL=https://hmis-api-production.up.railway.app
